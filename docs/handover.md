# Handover -- 2026-08-29T02:45Z, session spark (coordinator, `/apply --pool` wave 1)

## TL;DR
Wave 1 (`docs/plan.md`, 10 dispatch units) is fully merged, released as v1.19.0, and deployed live on the DGX. The next step is a sequential live-verification pass of all 9 fixes (plus a decision on #71's test-only coverage), then closing the issues for real. T1.6 (a pre-existing, unrelated deferred item) hit an unexplained DGX scheduling dimension and was parked rather than guessed at.

## Done & VERIFIED (observed evidence per line)
- All 10 wave-1 dispatch units merged into `main`, each independently re-verified by this session against `gh pr view`/`git log origin/main` (never trusted a subagent's own report): #77 PR #90 `b4bc9de`, #79 PR #92 `caa8dbf8`, #74 PR #93 `d0504a7a`, #88 PR #94 `a54c05c`, #80-signedness PR #95 `f30c7aa5`, #75 PR #96 `de7b90e`, #78 PR #97 `13042f7`, #71 PR #101 `e17199e`/`3aa3822`, #73 PR #103 `9821995`, #80-manifest PR #100 `f2859af`. T2.6 (flag-skip-list regression coverage) independently verified as already covered by pre-existing `TestBuildRunArgs_GPUDevicesWithOtherFlags` -- no new code needed.
- `go build ./...`, `go vet ./...`, `staticcheck ./...`, `go test ./... -race -timeout 120s -count=1` all clean on `origin/main` with everything merged (ran directly, 13 packages, this session).
- Released as **v1.19.0** (`gh release view v1.19.0` confirms real, non-draft assets) and **deployed live** on the DGX: `curl http://192.168.86.250:8080/healthz` -> `{"status":"ok","version":"1.19.0"}` (observed directly, before and after triggering `spark-auto-upgrade.service`).
- `docs/roadmap.md` fully reconciled: every wave-1 fix has a Shipped entry with root cause, fix, PR#, SHA, and an explicit live-verify caveat; the stale In-flight/Discovered-not-fixed sections cleared (PRs #98, #99, #102, #104, #105, all merged).
- `docs/plan.md` updated: every implementation/test task (T2.2-T2.9, T3.1-T3.4, T4.1-T4.10, T5.1-T5.4) marked `[x]` with a completion note; the merge/release/deploy/live-verify/close bundling tasks (T2.8, T3.5, T4.11, T5.5) deliberately left open since live-verify+close genuinely hasn't run (PR #106, this branch).
- `docs/devlog.md` consolidated: 9 new entries (one per fix) inserted at the top, newest-first, matching the file's existing convention (this branch).
- `docs/lore.md`: T4-7's same-indent-YAML entry (already merged via PR #100) plus a new entry this session added -- **GitHub can auto-close an issue twice**: once from the feature PR's "Fixes #N", again from the *next* release-please PR merge, because release-please's auto-generated changelog body inherits the same "closes #N" trailer from the original commit. Confirmed directly: #71 and #77 were both reopened after their feature PRs, then silently re-closed the instant PR #91 (the release-please PR) merged -- `closedAt` matched `mergedAt` to the second on both.
- All 23 wave-1 claims released cleanly (`T2.2`-`T2.9`, `T3.1`-`T3.5`, `T4.1`-`T4.9`, `T5.1`-`T5.4`) -- `/claim --list` / `git ls-remote origin refs/claims/*` should show none of these held.
- 6 merged worktrees removed and their branches deleted, local+remote, after verifying `git cherry origin/main <branch>` showed every commit as `-` (patch-equivalent, nothing unique): `spark-wave1-t2-4`, `spark-wave1-t2-6`, `spark-wave1-t2-9`, `spark-wave1-t4-7`, `spark-wave1-t4-9`, `.claude/worktrees/wave1-t4-3-issue75`. (T2-2, T4-1, T4-5 had already torn down their own worktrees before this pass.)
- 10 stray `kazi-partition/p-*` branches deleted from `origin` after confirming each had zero unmerged commits via `git cherry` (content either already on `main` or genuinely empty/false-green partitions from rejected kazi attempts) -- flagged as safe by T4-1, T2-2, and T4-7 independently plus verified by this session before deleting.
- Zero open PRs authored by this session (`gh pr list --author @me --state open` -> `[]`). Zero live kazi runs (`kazi status --json` -> `count: 0`).

## Done but UNVERIFIED (what still needs observing, and how)
**The entire live-verification pass is still outstanding.** All 9 fixes are merged, tested, and deployed to v1.19.0, but none has been confirmed against real running behavior on the DGX. Do this next, in order (lower-risk first, #88 last and alone):

1. **#77** -- POST a manifest with same-indent `containers:` (the issue's own shape), confirm the pod runs with a real container attached (not instant exitCode=0).
2. **#74** -- `curl -X POST $SPARK/api/v1/pods -H "Content-Type: application/json" -d @pod.json`, confirm `201` with a real pod created, and a malformed JSON body gets `400` not `201`.
3. **#79** -- reproduce 4+ lower-priority pods blocking a high-priority one behind the anti-thrash cap; confirm the (victim, requester)-scoped fix lets it preempt.
4. **#75** -- POST a Job (`backoffLimit: 0`, unresolvable image), confirm `status: "failed"` within seconds, not an endless retry loop. Recipe already in `docs/devlog.md`'s #75 entry / earlier agent report.
5. **#78** -- POST a pod requesting more CPU than the node has (deterministic pending), confirm `/logs` returns `200` empty (not `500`), `/events` surfaces the shortfall. Recipe in the #78 devlog entry.
6. **#73** -- reuse `/private/tmp/claude-501/.../scratchpad/repro-issue73.yaml` (or recreate from the issue: `bash -c` with nested double quotes) against the now-deployed v1.19.0; this session already proved the bug was real pre-fix using this exact manifest, so a clean run now is a real signal.
7. **#80 manifest** -- POST a pod, `GET .../manifest`, diff against the original bytes (recipe in T4-7's report / devlog entry); also check the 404 case.
8. **#80 signedness** -- construct a scenario that would have shown a negative free-GPU value pre-fix (a preemption/release race); confirm the shortfall message never goes negative. No exact recipe worked out yet -- may need thought.
9. **#71** -- **no live repro exists.** This is a podman-internal cgroup-cleanup timing race, not client-controllable input. Coverage is 4 fake-executor regression tests only. **Decision needed:** close on test-coverage alone, or attempt to trigger it by deleting several pods in quick succession under real DGX load (a clean run wouldn't prove the fix either way, only a repro would) -- next session's call, or ask David.
10. **#88** -- **HIGH RISK** (Risk Register R7: this exact bug already caused a 7-minute host-wide podman hang once). Do this alone, not concurrent with anything else on the DGX: submit one throwaway GPU pod, wait 10-20s, DELETE and time the response (expect completion within ~50s worst case), watch `timeout 10 sudo podman pod ps` in parallel, confirm no zombie `[podman] <defunct>` lingers under spark's PID afterward via `ps -eo pid,ppid,stat,etime,cmd`.

After each live-verify: reopen-check the issue first (`gh issue view <N> --json state,closedAt` -- it may have been auto-closed twice, see the lore.md entry above), then `gh issue close <N>` for real with a comment citing what was observed, update `docs/roadmap.md`'s caveat line for that fix, and flip `docs/plan.md`'s bundling task (T2.8/T3.5/T4.11/T5.5) to `[x]` once its whole group's live-verifies are done.

## In flight
None. No open PRs, no held claims, no live kazi runs, no worktrees left needing action (some ephemeral kazi-owned temp worktrees remain in `/private/var/folders/.../kazi-worktrees/` and `kazi-enforce-*` -- not this session's to manage, likely stale kazi internals or another concurrent session's; left alone).

## Blocked
- **T1.6** (live-verify the #76 CPU-overcommit bypass -- NOT one of wave 1's 10 dispatch units, a separate pre-existing deferred item). Deferred a 3rd time this session. History: 1st attempt blocked on DGX real load being 43-129+ on a 20-core host (genuinely oversubscribed, would have admitted a pod onto an already 2-3x contended host). 2nd deferral was the same finding restated. 3rd attempt: DGX load finally dropped to a safe 3.09/6.79 (5min avg), and accounted CPU was only 31% allocated -- so I tried filling accounted CPU via reservation-only sleep pods (no real load generated) to safely recreate the "accounted-full, real-load-low" condition the bypass is meant to handle. Hit an unexplained **`cpuset cores` shortfall** (`need 12 unassigned, 11 free`) that is a *different* resource dimension than the millicore accounting `/api/v1/resources` reports -- 20 cores total, only 11 free for core-pinning despite accounted CPU showing 5550/18000m allocated. Test pod (`t16-verify-fill`) was cleaned up (`DELETE` succeeded), DGX left in its prior state, no damage. **Unblocks on:** understanding how cpuset-core assignment interacts with the millicore-based overcommit-bypass logic before constructing a next test -- read `internal/scheduler`'s cpuset assignment code, or ask David whether this is worth the investigation time given it's not part of wave 1.

## Running processes left alive
None. No backgrounded kazi runs, no armed Monitors, no `ScheduleWakeup` pending (the `/loop /apply --pool` dynamic loop this session was running under should be considered stopped by this handover -- if the next session wants the loop to continue, restart it explicitly with `/loop /apply --pool`).

## Landmines & context
- **The fleet-wide worktree-pin cross-contamination bug** (confirmed by 9/9 wave-1 dispatch units, and once by this coordinator session directly): the harness's "primary working directory"/`EnterWorktree` pin is shared/racy across sibling teammates in the same team session, not per-agent isolated -- commands can silently execute in another agent's worktree, not just mislabel it. No data loss occurred anywhere this wave (every hit was caught via `pwd`/`git status` before mutating). Filed once via `SendFeedback`. **Workaround: never call `EnterWorktree` or bare `cd`; prefix every command with an explicit absolute path (`cd <path> &&` or `git -C <path> <cmd>`); verify `pwd`/`git rev-parse --show-toplevel` before every mutating command.**
- **GitHub double-auto-close via release-please** (new this session, see `docs/lore.md`): reopening an issue after its feature PR auto-closed it is not enough -- the next release-please PR merge can silently re-close it again if the original commit used a `closes #N` trailer. Check `closedAt` against the release PR's `mergedAt` after every release merge, for every issue this wave touched, not just the ones you expect.
- **kazi-org/kazi#1683**: `kazi apply --parallel` can wedge at startup with an idle-scheduler signature (hit 3x this wave: T4-1, T4-5 x2). Fix: kill the hung `beam.smp` process directly (not kazi's own self-reap, which has a worktree-deletion side effect per another comment on that thread), then fall back to direct implementation using the kazi-drafted predicate vector as the acceptance gate (`kazi apply --check --json`) instead of a 3rd redispatch.
- **kazi-org/kazi#1694 (filed this wave, T4-1) -- serious, read this before trusting any `kazi apply --parallel` verdict**: a `verdict: match_count` predicate (kazi-org/kazi#1690's own recommended fix for the zero-match false-green class) reported `pass` with the landing partition branch at **zero commits, empty diff**. `kazi status`'s aggregate `converged: true` was silently wrong; only manually inspecting the actual partition branch (`git worktree list` / `git branch -a`, grep `kazi-partition/`) and re-running the predicate command by hand caught it. `kazi apply --check --json` (observe-only) was NOT affected. **Never trust a `--parallel` "converged" verdict alone -- confirm the landing branch has a real, non-empty diff first.**
- **kazi-org/kazi#1693 (filed this wave, T4-3)**: a `custom_script` guard predicate using `n=$(... | grep -c pattern); test "$n" = 0` under `set -e` aborts on the assignment line *exactly when the count reaches the desired zero* (grep -c exits 1 on zero matches) -- never reaches the `test`, so it can never report pass for the success case. Reported as false `stuck`, not false-converged. Fix: `|| true` after the assignment, or don't use `grep -c` as a zero-is-success counter under `set -e`.
- **Two separate instances this wave of a kazi grind converging clean while silently substituting a weaker/wrong deliverable than the acc line required**: T2-2's first attempt (test proved nothing about real podman parsing, plus a false "already fixed" devlog claim) and T4-7's two attempts (JSON-reconstruction instead of literal bytes, twice, even past a negative-space predicate). Root cause both times: the capability predicate checked "a named test passes," not what the test actually asserts. When authoring `acc:`-derived predicates for a "return X byte-equivalent/behaviorally-equivalent to Y" requirement, write the test yourself (or checksum-guard it) rather than trust the grind to write an honest one.
- **T2-2's first kazi attempt also crossed two boundaries never mechanically enforced**: it touched `docs/plan.md`/`docs/roadmap.md`/`docs/devlog.md` (coordinator-owned this wave) and the DGX directly (centralized per Risk Register R6/R7), both stated only in dispatch prose, not in the predicate brief -- `bypassPermissions` gave it room to do both. Fixed on redispatch with diff-based guard predicates (forbidden-file check, DGX/SSH-string grep on the full diff), not just stronger wording.
- **Definition of done in this repo requires live-verification before closing an issue** (`docs/plan.md`'s Operating Procedure) -- merged+released+deployed is not done. Every wave-1 PR's default "Fixes #N" line auto-closes on merge regardless; the standing practice this session established is check-and-reopen after every merge (and now, after every release-please merge too, see the lore.md entry).
- Scratch tracking file with full blow-by-blow detail for every dispatch unit (verification steps, exact commands, agent quotes) lives at `.claude/scratch/wave1-status.md` -- gitignored, not on this branch, but still on this machine if more detail than this doc is needed. `.claude/scratch/wave1-claims.json` similarly (now stale -- all claims released).
- `/private/tmp/claude-501/-Users-dndungu-Code-feza-ai-spark/dfba2d8d-408e-4883-a40e-8ad3f6d2eefd/scratchpad/repro-issue73.yaml` -- the exact repro manifest used to prove #73's bug live, reusable for its post-deploy live-verify.

## How to resume
1. `git fetch origin && git checkout handover` (or `wave-1-handover` if `handover` doesn't exist yet -- this session pushed to `wave-1-handover` and opened PR #106; check whether it merged).
2. Read this file, then `docs/roadmap.md` and `docs/plan.md` for current state.
3. Nothing to claim -- wave 1's lanes are all released and done except the live-verify pass, which is coordinator-centralized work (per Risk Register R6/R7), not something to fan out via `/claim` again.
4. Start the live-verify pass (see "Done but UNVERIFIED" above), lowest-risk first, #88 last and alone.
5. Once all 9 are verified: close issues for real, flip `docs/plan.md`'s remaining `T2.8`/`T3.5`/`T4.11`/`T5.5` checkboxes, decide #71's test-only-coverage question if not already decided.
6. T1.6 is a separate, lower-priority thread -- pick it up only with explicit direction (it's not part of wave 1), starting from the cpuset-core landmine noted above.
